import { ScrollView } from "react-native";
import MainHeader from "@/components/layout/MainHeader";

function ManagerRentalPage() {
    return <ScrollView>
        <MainHeader title={"대여 요청 관리"} />
        ManagerRentalPage</ScrollView>;
}
export default ManagerRentalPage;
